# 我的工程 · 实施与覆盖契约

2026-09-05；修订 1；基线 ffc03aa；分支 codex/project-library。

用户确认实现多工程管理：新建、另存为、复制、缩略图列表、切换、保存版本与恢复。保留原布置、短片、作品展示和发布包，不增加账号、云同步或删除功能。普通可逆选择自主实施；不覆盖既有存档，不申请新权限或费用。

以上为初次交付范围。用户随后要求工程删除和不保存直接返回，增量实现与验证见 [PROJECT-ACTIONS.md](PROJECT-ACTIONS.md)；此记录保留原基线验收。

架构为 Hybrid Workspace：工程库使用清晰的卡片列表；进入具体工程后保留现有 WebGL 操作空间。暖白/苔绿、与原应用一致的字体和按钮；真实场景缩略图，未能渲染时明确显示缺少预览。主流程：迁入旧存档 → 工程 A 编辑与保存 → 另存为 B → 创建 C → 各视图切换 → 版本恢复 → 刷新 → 分别导出并检查隔离性。

本地 IndexedDB 存储每个完整工程与保存快照；恢复追加版本，不覆盖旧快照。原 localStorage 保留；按来源与内容去重迁入，快速工作区的后续新保存独立保留。工程 ID 随房间/短片/作品展示路由传递；无 ID 的旧入口保留快速存档兼容。并发保存以版本校验防止静默覆盖。无数据库、空间不足、无效导入均明确反馈。

真实运行：npm run dev，http://127.0.0.1:5173/；生产验收 npm run build / npm run preview，4173。用户现有 4180 是此前导出站，与本轮作者功能分开。

| 要求/状态 | 证据 | 状态 | 下一步 |
| --- | --- | --- | --- |
| 原版基线 | .scratch/projects/baseline.json/png；默认场景与界面已查看 | pass | — |
| 新建、复制、另存为、列表与搜索 | journey.json、截图 01、04；真实操作到第五套工程 | pass | — |
| 旧房间/短片迁移且原存档不变 | migration.json；原始字节保留，重开去重 | pass | — |
| 三套工程切换，房间/短片/作品一致 | A/B/未完成 C/副本，数据逐字段核对 | pass | — |
| 保存版本与非破坏性恢复、刷新 | 截图 02；恢复后再恢复，所有版本保留 | pass | — |
| 并发、无效 JSON、数据库不可用/配额失败 | projects.spec.ts 3–5；真实事务回滚，另存为成功 | pass | — |
| 1440×900、1280×800、窄屏管理、键盘 | 六张最终截图；删除按钮未越出 769px，键盘焦点通过 | pass | — |
| 每工程导出 JSON/网站包和旧能力回归 | A/B ZIP 独立运行；34 项全回归和 5 项 Pages 通过 | pass | — |
| 运行环境/性能/限制/启动与 GitHub 记录 | PROJECTS.md / PROJECTS-VALIDATION.md / acceptance.json | pass | — |

本轮不扩展公网部署、真实手机 GPU、Safari/Firefox 兼容范围。截图最多六张；保留实际验收文件与机器可读记录。完成后只处理验收问题。
