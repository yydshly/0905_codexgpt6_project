# 第二轮优化 · 真实运行记录

2026-09-05，响应「继续优化」。源码提交：[`42fc201`](https://github.com/yydshly/0905_codexgpt6_project/commit/42fc201)；镜头/拾取修正：[`9ccc492`](https://github.com/yydshly/0905_codexgpt6_project/commit/9ccc492)。保留单房间、8 类物件、3 种氛围和 JSON v1 格式，本轮没有新增依赖或外部资产。

## 实际改变

| 基线问题 | 本轮调整 | 验证结果 |
|---|---|---|
| 椅背近似平板，植物叶片近似厚圆片 | 制作弧面椅背、宽薄曲面叶片和叶脉；调整扶手连接 | 默认、近景、俯视及椅子旋转后的正面均实际检查，材质缩略图使用相同几何 |
| 椅脚、书架、花盆接地留有小间隙 | 调整几何底部与支撑高度；椅子判断地毯支撑时考虑地毯旋转 | 默认与近景接触正常；仍沿用基本支撑规则，无物理模拟 |
| 台灯几何高于标注的 0.42 m | 调整灯杆和灯罩，使灯具实际高度、宽度与属性范围相符 | 场景和缩略图同步；添加、拖动、旋转、调光、保存恢复通过 |
| 选中角标偏细，点击前缺少抓取反馈 | 加强角标，显示物件名称；拖动时显示实际坐标；可抓取物件使用抓取光标 | 双尺寸检查，提示跟随物件且不拦截鼠标；观察模式提示同步 |
| 手动镜头变化后，原预设仍高亮 | 根据真实相机位置、目标和缩放计算高亮；补齐模式状态及缩放边界反馈 | 手动观察/缩放取消高亮，俯视与自定义镜头保存刷新后状态正确 |
| 键帽、毯穗、地板等逐件绘制，观察也重算阴影 | 同一家具内部合批重复几何；仅几何或光照方向变化时更新阴影 | 镜头观察绘制调用减少 76.81%；合批后仍准确选择宿主家具，材质和删除撤销正常 |
| 选中与导出的隔离需要更强验证 | PNG 生成期间暂停普通绘制，隐藏 3D 辅助标记 | 通过浏览器分别下载选中/未选中图片，两份 PNG 逐字节一致 |

## 浏览器证据

最终源码的 **8/8** 真实浏览器测试通过，完整运行 **31.6 秒**。原 6 项操作验收全部保留；新增 2 项覆盖镜头/选择反馈及合批后的拾取/导出。所有编辑仍通过鼠标、键盘、表单、浏览器上传和下载执行。测试接口仅读取数据及投影位置。

实际视觉检查覆盖 1440×900、1280×800，默认、近景、俯视、深夜与选择状态。五张页面截图和一张应用导出图片均已更新：

- [默认 1440×900](evidence/01-default-1440x900.png)、[近景 1440×900](evidence/02-close-1440x900.png)
- [俯视 1280×800](evidence/03-top-1280x800.png)、[深夜选中 1280×800](evidence/04-night-selected-1280x800.png)
- [窄屏物件库](evidence/05-narrow-library-390x844.png)、[实际 PNG 下载](evidence/06-exported-scene.png)

新增毯穗拾取用例首次点在两条毯穗之间的空隙，因此未选中；修正测试点击位置到可见毯穗中心后通过。没有放宽物件 ID、选中状态或文件一致性的断言。

最终视觉复查看到选中标签在切换近景后偏移：已使相机矩阵和标签在预设应用时立即更新，并补充标签锚点像素断言；从自定义缩放回到同位置预设时也正确更新。GitHub 首轮 7/8 通过，新增键帽拾取在切换俯视后失败；同样修正了拾取前的世界矩阵更新，并让验收等待已请求的画面绘制完成后再点击。

一次本地主流程在 Playwright 将已下载 JSON 复制到证据目录时遇到 Windows `copyfile UNKNOWN`，应用内下载已产生；未修改或放宽该流程，随后完整流程及最终 8 项套件重新执行通过。

## 性能对比

真实环境为 Windows 11、Chrome 152.0.7977.82 headless、Intel UHD / ANGLE D3D11、DPR 1。性能采样视口 1280×800，实际画布 814×603。对照是前一交付提交 `aa4eb11` 的原始记录，未重新运行旧版，时间差异可能包含机器负载影响。

| 指标 | 前一交付 | 本轮 |
|---|---:|---:|
| 镜头观察时多通道绘制调用 | 1,505 | 349（减少 76.81%） |
| 场景 Mesh 数 | 409 | 171，含 15 个实例批次 |
| 场景三角形，按实例数计算 | 96,774 | 94,942 |
| 白昼平均 / P95 帧间隔 | 9.30 / 20.90 ms | 4.43 / 8.40 ms |
| 深夜平均 / P95 帧间隔 | 8.25 / 16.70 ms | 4.30 / 4.30 ms |
| 本地导航到 ready | 639 ms | 1,011 ms |
| 静止 1 秒新增绘制 | 0 | 0 |

每种氛围采集 100 个 requestAnimationFrame 间隔，丢弃前 5 个；包含真实观察拖动。它们是短时间帧间隔，不能当作 GPU 渲染耗时或所有机器的帧率保证。349 次对应缓存阴影后的观察画面；移动家具或切换氛围会重新绘制阴影。启动时仍生成所有材质缩略图，本轮没有宣称启动性能提高。

原始记录：[performance.json](evidence/performance.json)、[optimization.json](evidence/optimization.json)。完整文件闭环记录：[journey.json](evidence/journey.json)。

## 工程记录与边界

TypeScript、生产构建、8 项浏览器验收、`git diff --check` 均通过；`npm audit --audit-level=high` 为 0 漏洞。产物约 674 kB JavaScript / 201 kB gzip；Three.js 及后处理的分块体积提示仍保留。

GitHub Actions：[初次 7/8 运行](https://github.com/yydshly/0905_codexgpt6_project/actions/runs/33930584456)、[修正后代码验证](https://github.com/yydshly/0905_codexgpt6_project/actions/runs/33930945834)，最终远端结果将在完成后记录。CI 的 Linux SwiftShader 测量与上述本地 GPU 数据分开。

单个本地存档、无家具间完整碰撞、无灯具独立投影、手机完整属性编辑和跨浏览器尚未覆盖的边界沿用第一版，见 [验收报告](VERIFICATION.md)。
