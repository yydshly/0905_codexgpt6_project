# 家具与 3D 复用增量

用户已确认：可替换照片、沙发、床、整屋 / 单件 GLB。追加问题是网页及其他产品的二次开发方式；提供准确接入文档与可运行的独立查看器示例，不扩展账号、云服务或付费发布。独立分支 `codex/reusable-study`，基于 `b360242`。

沿用原有 Immersive / Spatial Stage、房间与短片渲染器。默认构图保持，固定墙画转为可编辑物件。沙发、床作为可选物件，不堆入默认房间。照片限制在背墙，提供水平 / 高度拖动、尺寸、画框与本地上传，图片随 JSON 保存。新增数据版本迁移，旧存档保持原始字节。

运行：`npm run dev`，http://127.0.0.1:5173/；验收生产构建 http://127.0.0.1:4173/。桌面 1440×900、1280×800，白昼 / 深夜、默认 / 近景 / 俯视。2026-09-05 基线截图已检查于 `.scratch/reuse-before.png`。

| 范围 | 验收 | 状态 |
|---|---|---|
| 家具 | 沙发与床真实模型、缩略图、添加移动旋转材质、历史 | pass |
| 墙画 | 可选可拖动、上传照片、尺寸画框、错误不改工程 | pass |
| 数据 | 房间 v2 / 短片 v3，旧文件迁移，保存刷新与照片 JSON 往返 | pass |
| GLB | 整屋与单件真实下载，独立 GLTFLoader 打开照片 / 材质 / 比例 | pass |
| 二次开发 | 源码接入文档、查看器示例、明确展示 / 工程 / 模型边界 | pass |
| 回归 | 本地 20 项、追加旧 v2 迁移、Pages 4 项；最终 GitHub 软件渲染 21/21 通过 | pass |
| 交付 | 真实浏览器截图、GLB 文件、测试环境与限制、GitHub 分支提交 | pass |
