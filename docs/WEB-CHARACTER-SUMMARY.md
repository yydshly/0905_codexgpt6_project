# 新增网页能力与角色导览阶段归纳

2026-09-06。当前开发分支：`codex/character-guide`。本文以已推送的功能提交 `0dc2aed` 为基准整理网页入口、数据关系、交付和限制；后续人物质量建议单独列为待实施内容。

## 网页入口与用途

本地启动：`npm ci` → `npm run dev`，地址为 `http://127.0.0.1:5173/`。以下页面属于同一个应用，通过 `workspace` 参数切换。

| 页面 | 路由 | 已实现的用途 |
| --- | --- | --- |
| 我的工程 | `?workspace=projects` | 多工程、新建、复制、另存为、保存版本、恢复、备份、删除、使用帮助 |
| 布置书房 | `?workspace=room` | 编辑 11 类物件，设置家具、材质、灯光与作品入口 |
| 镜头短片 | `?workspace=film` | 最多三个镜头段、时间轴、保存、真实视频导出；无参数时默认打开此页 |
| 作品展示 | `?workspace=portfolio` | 点击物品查看作品、打开项目链接、设置并发布个人展示页 |
| 角色导览 | `?workspace=guide` | 个人 IP / 旧角色切换、行走、阅读、介绍、坐下站起、导览编辑和导出 |
| 网页接入示例 | `?workspace=integration` | 展示宿主网页控制播放器并接收作品点击事件的接入方式 |
| 嵌入播放器 | `?workspace=embed` | 供宿主网页嵌入和控制，具体协议见复用文档 |
| GLB 查看器 | `?workspace=viewer` | 打开导出模型及对应配置，验证模型复用与作品入口 |

已有网页能力详见 [工程库](PROJECTS.md)、[使用帮助](PROJECTS-HELP.md)、[工程删除与不保存返回](PROJECT-ACTIONS.md)、[作品网站发布](PUBLISH.md)、[网页复用接口](REUSE.md)。这些既有能力与本次角色导览共同组成产品，不应全部归为最近一次提交的新功能。

## 新增角色网页怎样关联原工程

工程库顶部提供「角色导览 · 试验」入口。要带入正在展示的房间和作品，在「作品展示」顶部点击同名入口，会生成该房间与作品配置的独立导览快照。

房间、镜头和作品设置属于原工程；导览基于进入时的快照继续编辑，保存到自己的本地键。导览保存不会自动覆盖原工程，原工程后续修改也不会实时同步到已保存导览。更新导览房间可重新导入 JSON 或从作品展示重新进入。

带 `snapshot=...` 的地址依赖原浏览器中的数据，不能当成公开分享链接。其他设备打不开该本地快照时会显示提示和示例；分享给访客应使用实际导出并部署的网站包。本机、线上、不同浏览器的本地存档彼此独立。

## 角色导览网页已补齐的内容

- 左侧角色选择与段落列表，中间真实 3D 预览，设置面板和底部时间轴；提供角色近景、恢复镜头及三种光照氛围。
- 18 岁成年形象的个人 IP 设定 01 样版，保留旧青年角色。外观仍为样版，未使用本人照片，不承诺肖似本人。
- 新建示例默认 28 秒：阅读 8 秒、介绍 8 秒、坐下阅读并站起 12 秒。最多三个段落。
- 已有导览保留原段落；可通过「＋ 添加坐下 / 站起段落」加入座椅动作，选择椅子、修改时长、查看坐下 / 坐姿 / 站起、移除和交换顺序。
- 行走按路程驱动迈步，转身配合抬脚；坐姿对齐实际椅子，修正髋部高度和脚部接触；起身前收起手册。
- 播放、暂停、拖动、撤销重做、本地保存刷新、JSON 导入导出、真实视频及独立导览网站包。
- 角色设置升级为 v4，兼容 v1/v2/v3。导入错误保留当前工程；动作加载、通道阻挡或时长不够时明确提示，禁用无法完成的播放 / 视频生成。

实现与资产来源见 [个人 IP 接入](character-ip/INTEGRATION.md) 和 [人物动作升级](MOTION-GUIDE.md)。原始人体骨架及基础动作来自 Quaternius CC0；项目制作与外部资产的归属分别记录。

## 怎样带到其他网页或产品

| 成果 | 可以做什么 | 必须了解的边界 |
| --- | --- | --- |
| 独立作品网站 ZIP | 发布房间、作品介绍和可点击入口 | 解压部署后才有公开网页；更新需重新导出部署 |
| 独立导览网站 ZIP | 发布带人物、动作和作品入口的导览页 | 附带所选角色与动作资源，共用导览运行时；访客页不提供作者编辑和保存功能 |
| 房间 GLB + 对应 JSON + 适配代码 | 在其他网页渲染房间并接入作品点击 | GLB 本身不自动执行网页业务逻辑；不同渲染环境的效果可能不同 |
| 人物 GLB + 动作资源 + 导览运行时 | 二次开发角色展示及导览 | 单独人物 GLB 不包含完整导航、坐姿对齐、手册和网页点击逻辑 |
| PNG / MP4 / WebM | 在网站中作为图片或视频展示 | 不包含可编辑 3D 或物件点击行为 |
| 工程 JSON | 备份、迁移、继续编辑 | 不等于托管网站，也不包含整个本地工程历史库 |

代码、角色 GLB、动作 GLB、构建脚本和验收文件均提交在开发分支中。`public/site-kit` 是由构建脚本生成的发布模板，不作为手工维护的源文件入库。日常运行无需 Blender、模型服务或 API Key。

## 已有运行证据与当前发布状态

功能提交 `0dc2aed` 的本地浏览器验收为 13/13，通过导览、坐姿和作品网站发布三套测试。环境为 Windows 11 / Chrome 152，覆盖 1440×900、1280×800；实际导出 26.5 秒、1280×720、30 fps H.264/MP4，并重新打开播放到结束。26.5 秒来自验收中修改第二段时长，不是默认示例时长。

- [完整验收、环境、性能和截图](motion-guide-evidence/README.md)
- [实际导出视频](motion-guide-evidence/xiaohe-guide.mp4)
- [对应工程 JSON](motion-guide-evidence/verified-guide.json)
- [可独立部署的角色导览网站包](motion-guide-evidence/xiaohe-guide.website.zip)

本次归纳提交只更新说明文档，引用已有证据，不代表重新运行了全部浏览器测试。未验证的编码、浏览器和人物接触限制以验收记录为准。

**推送分支不会自动更新公开站点。** 现有 GitHub Pages 工作流仅在 `main` 手动触发，七组浏览器套件通过后才发布。角色导览分支目前未合并、未部署到线上主站；当前 GitHub 提交用于保存源码、成果和说明。

## 人物质量的下一步

已完成的操作链路不等于人物美术达标。模型、蒙皮和动画表演仍是主要不足。正式角色制作、统一动作与独立样片验收的建议见 [人物质量升级路线](CHARACTER-QUALITY-ROADMAP.md)；该路线尚未实施，不应作为已交付功能宣传。
